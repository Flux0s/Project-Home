import React, { Component } from "react";
import { withStyles } from "@material-ui/core/styles";

const styles = (theme) => ({
});

class SpotifyPage extends Component {
  render() {
    return (
      <div> Spotify Settings </div>
    );
  }
}

export default withStyles(styles)(SpotifyPage);
